OUTPUT_DIR=/data/pdf_artifacts
PUBLIC_BASE_URL=http://localhost:8011/artifacts

# Optional on Windows if PATH is not inherited by uvicorn.
# Use the full executable path, not only the folder.
# Example: TECTONIC_PATH=C:\Program Files\tectonic\tectonic.exe
TECTONIC_PATH=
LATEX_ENGINE_PATH=
PDFLATEX_PATH=
