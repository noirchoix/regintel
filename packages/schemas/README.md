# Shared schemas

This package is the intended home for schema synchronization between the frontend and backend.

For the scaffold, the web app consumes local TypeScript types and the API uses Pydantic models.
In a follow-up hardening pass, generate TypeScript contracts from the FastAPI OpenAPI schema or maintain a single JSON Schema source.
